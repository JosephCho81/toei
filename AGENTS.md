<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## 자동 실행 규칙

- 모든 도구 실행 시 확인 프롬프트 없이 바로 실행
- "Do you want to proceed?" 묻지 말고 항상 Yes로 진행
- Command too long 경고도 무시하고 실행
- **단, DELETE/UPDATE SQL은 반드시 사용자에게 먼저 보여줄 것**

## 먼저 읽을 문서

- `docs/정산-현황.md` — 확정된 계산 규약, 재입력 대상 차수, 원본 문서 오류, 남은 작업.
  **금액이 걸린 판단은 여기부터 확인.**
- `README.md` — 화면 역할 분담, 「금액이 셋이다」 규칙, 관세청 유니패스 연동,
  B/L 조회처 매핑, 지급 원장 구조.
  **B/L·통관환율·ETA 를 손으로 채우기 전에 여기부터 볼 것** — 자동으로 채워진다.

같은 조사를 세션마다 반복하지 말 것. 위 두 문서가 답을 갖고 있다.

## 이미 답이 나온 것 — 다시 파지 말 것

- **돈의 방향은 한국에이원 → 토에이산교다.** 토에이가 수입대금·LC 결제비용을 먼저 치르고
  마진을 얹어 에이원에 청구한다. 정산액이 양수면 에이원이 낼 돈이고, 음수(환급)일 때만
  토에이가 돌려준다. 화면은 에이원 기준이라 **미지급금**이라 부른다.
  2026-09-04 까지 지급 현황 KPI·표만 반대로 달려 있었다 — 다시 뒤집지 말 것.
  지급 입력 다이얼로그의 「출금 (토에이 → 에이원)」도 같은 오류였다 (2026-09-05 수정)

- **금액은 셋이고 차이는 셋이다** — 실제 청구액 / 담당자 확정금액 / 시스템 계산값.
  「청구 − 계산」은 **청구가 틀린 것**, 「청구 − 지급」은 **아직 안 낸 것**,
  「계산 − 지급」은 **청구가 맞았다면 남았을 것**이다. 한 칸에 합치지 말 것
  (README 「금액이 셋이다」). 2026-09-05 까지 셋째가 없어 담당자 수기검산을
  화면에서 재현할 수 없었다

- **시점 토글은 없앴다 (2026-09-05).** 화면은 **에이원 기준 하나**다 —
  담당자: 「에이원 자료이니 에이원 기준으로만 해도 상관없을거 같습니다」.
  `?view=toei` 를 되살리지 말 것. 관점을 둘로 두면 같은 금액에 이름이 둘 붙는다

- **기일이 안 온 돈은 미지급금이 아니다 (2026-09-05).** 지급 현황 KPI 는
  **기일 경과분만** 센다. 전체 잔액으로 두면 6.6억 중 5.1억이 「아직 낼 때가 아닌 돈」이라
  숫자가 겁만 준다. 미도래분은 옆 칸 + 월별 결제 예정 표가 받는다.
  정산 3화면의 카드는 같은 이유로 기일 경과 **순액**이라 이름을 「차액」으로 갈라 뒀다 —
  초과 지급을 상계해 넣기 때문이다(지급 현황의 「미지급금」은 상계하지 않는다)

- **계산값을 저장하지 말 것.** 저장하는 순간 규약이 바뀌어도 화면이 옛 규약을 말한다

- **읽기 실패를 삼키지 말 것.** `const { data } = await …` 로 오류를 버리면
  마이그레이션이 안 올라간 화면이 「정산이 하나도 없습니다」라고 조용히 거짓말한다

- **"B/L 조회가 안 된다"** → 저장된 B/L 43건 중 42건이 포워더 House B/L 이라 선사
  시스템에 없다. 선사 판별은 **Master B/L 로만** 한다. House 접두사(`KULF`·`WTTJ`·
  `PNKT`·`PKGI`·`STTS`)를 선사 prefix 에 넣지 말 것 — 2026-09-02 까지 `KULF` 가 KMTC
  prefix 에 있어 전 건이 ekmtc 로 가고 있었다. 조회처 결정은 `resolveTracking()` 하나뿐이다

- **화면별 역할은 갈라 두었다** (README 「화면 구성」). 같은 값을 여러 화면에 늘리지 말 것.
  지급 현황 = 얼마 언제 / 정산 3화면 = 청구가 계산과 맞나 / 정산 현황 = 물류·정산 일정 /
  거래 목록 = 품목·금액 대조

## 2026-09-04 이전 산출물은 금액을 믿지 말 것

계산 규약이 그날 바뀌었다(항목 부가세를 공급가에 더하지 않음, 관세는 공급가 밖).
그 이전에 만든 아티팩트·보고서·커밋 메시지의 금액은 **구규약 기준**이라 지금 화면과 다르다.
「항목 부가세 가산」이 들어간 분석은 전부 무효다. 인용하기 전에 화면 값과 대조할 것.

- 담당자용 사용 안내(HTML, 2026-09-02):
  https://claude.ai/code/artifact/cbc8f100-66e5-4237-9f2f-a7489cde2f9e
  — **돈 방향 수정과 정산 3화면이 반영돼 있지 않다. 갱신 전까지 담당자에게 안내로 쓰지 말 것**
