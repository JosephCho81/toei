<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## 자동 실행 규칙

- 모든 도구 실행 시 확인 프롬프트 없이 바로 실행 (「Do you want to proceed?」 묻지 말 것)
- Command too long 경고도 무시하고 실행
- **단, DELETE/UPDATE SQL 은 반드시 사용자에게 먼저 보여줄 것**

## 문서 — 필요한 파일 하나만 읽는다

**전부 읽지 말 것.** 문서는 질문 하나에 파일 하나로 갈라 두었다 (2026-09-10).
한 번 읽은 것은 그 세션 내내 매 턴 재전송되므로, 안 쓰는 줄이 곧 비용이다.

| 무엇을 물을 때 | 파일 |
|---|---|
| 금액이 어떻게 계산되나 | `docs/계산-규약.md` |
| 이 차수가 왜 안 맞나 | `docs/차수-이슈.md` (차수 번호로 grep) |
| 이거 왜 이렇게 돼 있나 | `docs/결정-이력.md` |
| 지금 뭐가 남았나 | `docs/남은-작업.md` |
| DB·코드의 함정, 조사 방법 | `docs/시스템-상태.md` |
| 화면을 고쳐도 되나 | `docs/화면-규칙.md` |
| 지급 입력·배분·잔액 | `docs/지급-원장.md` |
| 통관일·환율·선사·B/L | `docs/추적-연동.md` |
| 스키마·마이그레이션 | `docs/마이그레이션.md` |
| 세션 비용 | `docs/토큰-절감.md` |

60줄이 넘는 파일은 `grep -n '^#'` 로 목차를 먼저 보고 `sed -n` 으로 그 절만 가져온다.
전체 색인은 `docs/정산-현황.md`, 프로젝트 소개는 `README.md`.

**같은 조사를 세션마다 반복하지 말 것.** 답은 이미 위 파일들에 있다.

## 이미 답이 나온 것 — 다시 파지 말 것

한 줄씩만 적는다. 이유가 필요하면 `docs/결정-이력.md` 를 읽는다.

- **돈의 방향은 한국에이원 → 토에이산교다.** 양수면 에이원이 낼 돈이고, 음수(환급)일 때만
  토에이가 돌려준다. 화면은 에이원 기준이라 「미지급금」이라 부른다. 라벨을 뒤집지 말 것
- **금액은 셋, 차이도 셋이다.** 실제 청구액 / 담당자 확정금액 / 시스템 계산값.
  한 칸에 합치지 말 것
- **계산값을 저장하지 말 것.** 저장하는 순간 규약이 바뀌어도 화면이 옛 규약을 말한다
- **시점 토글은 없앴다 (2026-09-05).** `?view=toei` 를 되살리지 말 것
- **남은 돈은 넷으로 가른다 (2026-09-10).** 연체(달 넘김) · 이번 달 지급 중 ·
  지급금 차이(35차까지) · 기일 미도래. 경계는 차수(35)로 자르지 기간으로 자르지 않는다
- **36차부터 엑셀 청구금액을 비웠다 (2026-09-10).** 청구값을 넣기 전까지 계산값이 잔액 기준이다
- **최종정산은 29차부터 신방식(공급가 100원 절사).** 1~28차는 구방식 그대로 둔다
- **읽기 실패를 삼키지 말 것.** `const { data } = await …` 로 오류를 버리면
  화면이 「정산이 하나도 없습니다」라고 조용히 거짓말한다
- **선사 판별은 Master B/L 로만 한다.** House 접두사(`KULF`·`WTTJ`·`PNKT`·`PKGI`·`STTS`)를
  선사 prefix 에 넣지 말 것. 조회처 결정은 `resolveTracking()` 하나뿐이다
- **규약의 원본은 `lib/calculations/` 다.** 화면·PDF·대시보드는 호출만 한다.
  공식을 다시 쓰면 두 벌이 어긋나고, 어긋난 쪽이 보고서로 나간다
- **2026-09-04 이전 산출물의 금액은 구규약 기준이라 지금 화면과 다르다.**
  「항목 부가세 가산」이 들어간 분석은 전부 무효다
