# 정산 현황 및 확정 규약

최종 갱신: 2026-09-01 · 라이브 DB 전수 대조 + 감사 로그 추적 + 담당자 수기 검산 대조 + 계산 코드 전수 점검 완료 기준

세션마다 같은 조사를 반복하지 않기 위한 문서. **금액이 걸린 판단은 여기부터 확인한다.**

---

## 1. 확정된 계산 규약 (담당자 확인 완료)

### 중간정산

```
공급가 = 수입금액USD × (1 + 마진%) × 통관환율
       + 해상운임 + 통관비 전체
       + 항목 부가세          ← 국내 비용(트럭운송비 등)에 붙은 부가세
       − 수입부가세           ← 세관 납부분, 매입세액공제 대상
부가세 = 절사된 공급가 × 10%
합계   = 공급가 + 부가세
```

항목별로 부가세를 떼지 않고 **토에이 실지급액을 그대로 매입원가**로 잡는다. 세관에 낸 수입부가세만 공제로 회수하므로 그것만 뺀다.

**포워더 인보이스의 「TOTAL AMOUNT」는 부가세 포함이다.** DB는 항목을 **공급가로 저장하고 `is_vat_taxable`로 부가세를 표시**하며, 계산에서 `itemVat`으로 다시 더한다. 그래서 `중간정산.csv`의 운송비 열(= 인보이스 총액)과 DB 항목 합계가 다른 것은 **정상**이다 — 차이가 곧 항목 부가세다. 이걸 「금액 누락」으로 오판하지 말 것.

- 예) 2차 운송비: 문서 3,498,614 = DB 3,440,168 + 과세 4건(T.H.C 18,000 · DOCUMENT FEE 5,000 · D/O CHARGE 6,046 · TRUCKING FEE 29,400) 58,446
- 단 `inclusive` 모드는 `itemVat`을 더하지 않으므로 이 부가세가 통째로 사라진다 (2차 +58,400의 원인)

### 최종정산(클로징)

```
환차손익 = 원금 × 통관환율 − LC 결제비용        (양수 = 환차익)
공급가   = ((LC 부대비용 − 환차손익) + 기타 미정산 비용(A+B+C)) × A1 부담비율
부가세   = 절사된 공급가 × 10%
합계     = 공급가 + 부가세
```

- **부호**: 지불 양수 / 환급 음수
- **A1 부담비율**: 34차까지 50%, 35차부터 100%. 35차 이후 클로징이 아직 없어 소급 대상 없음
- **기타 미정산 비용(A+B+C)에도 부담비율을 곱한다.** 원본 문서가 이 항목만 부담비율 밖 전액으로 둔 것이 오류였다
  - `lib/calculations/closing.ts`의 `a1ClosingCostsKrw`. 신·구방식 모두 적용
  - `closingCostsTotalKrw`(원금)는 항목 목록 표시용, 공급가에는 부담분만 들어간다
  - A+B+C ≠ 0 인 차수는 **1·2차뿐** (라이브 DB 전수 확인)

### LC 결제비용 — 선지급금 별도 환율

```
LC결제비용 = round(선지급$ × 선지급환율) + round(잔액$ × 클로징환율)
```

구간별로 반올림해 더한다. 은행이 구간별로 결제해 청구하므로 합계를 한 번에 환산하면 원 단위로 어긋난다.
선지급금이 0·NULL이면 기존과 동일(총액 × 클로징환율)이라 기존 정산 계산은 변하지 않는다.

### 절사

- 0 방향 버림 = 절대값 기준. 음수(환급)도 동일
- 절사는 **공급가**에 건다. 합계에 걸면 공급가 + 부가세 ≠ 합계가 되어 세금계산서가 안 맞는다
- 적용 시작: 중간정산 17차부터 / 최종정산 29차부터
- **소급하지 않는다.** 과거 차수는 저장된 정책 스냅샷 그대로 둔다
  - 소급 시 변동폭 −98 ~ +99원인데, 지금 문서와 정확히 맞는 차수(3·5·9·19차 등)가 오히려 틀어지고 이미 발행된 세금계산서와 어긋난다
  - 29~34차는 전부 `floor_100` 으로 정리 완료

---

## 2. 미해결 — 6개 차수 (DB 확정금액 − 시스템 재계산)

| 차수 | 중간정산 | 클로징 | 막고 있는 것 |
|---|---|---|---|
| 1차 | — | +21,900 | 재입력 (부담비율 규약 적용) |
| 2차 | +58,400 | +166,700 | 중간정산: **`inclusive`가 항목 부가세 58,446을 버린 것** — 항목·플래그는 이미 정확하므로 `vat_mode`만 `exclusive`로 바꾸면 살아난다(**재입력 불필요**). 클로징: 부담비율 재입력 |
| 6차 | — | −100 | 중간정산 **해결 완료**(09-01) / 클로징: 음수 절사 — 시스템이 맞다. 확정금액만 맞추면 됨 |
| 7차 | — | −2,462,000 | 중간정산 **해결 완료**(09-01). 클로징만 원인 미규명 — 원본 서류 필요 |
| 23차 | — | −57,812,175 | 원인 규명 완료. 이자 −3,803 정정 포함, **금액이 커서 담당자 컨펌 대기** |
| 37차 | −44,880 | — | 과세항목 부가세 40,810 누락 — **시스템이 맞다**. 확정금액만 맞추면 됨 |

### 23차 — 두 건이 겹쳐 있다

**(가) 확정금액 미갱신 — 5,781만원.** 원인은 감사 로그로 확정됐다. 08-23 INSERT 시 LC결제비용이 11,660,589원으로 잘못 들어갔고, 08-24에 116,773,626원으로 정정했으나 확정금액 −59,350,700원이 갱신되지 않았다.

**(나) 이자 입력값 −3,803원.** 이자에 클로징환율 1,392.40이 아니라 **1,390.40(24차 은행정산 환율)이 적용**됐다. $1,900.26 × 1,390.40 = 2,642,122 ≈ 저장값 2,642,119. 차수 혼재(3번)의 흔적이 이자 한 줄에 남은 것이다.

| | 개설 | 인수 | 이자 | 합계 |
|---|---|---|---|---|
| 서류 우측(정본) | 193,085 | 632,193 | 2,645,922 | **3,471,200** |
| DB 입력값 | 193,085 | 632,193 | 2,642,119 | 3,467,397 |

재계산값 (`calculateClosing` 실제 실행):

```
현재 DB       부대비용 3,467,397  환차익 6,264,715  →  확정 -1,538,525
(나) 정정 후   부대비용 3,471,200  환차익 6,264,715  →  확정 -1,536,433   (+2,092)
```

**(나)를 먼저 넣고 (가)의 목표값을 −1,536,433으로 잡는다.** 두 SQL은 한 트랜잭션으로 실행할 것 — 절사 소급 안 하므로 `rounding_policy`는 `none` 유지:

```sql
UPDATE lc_fee_items SET amount_krw = 2645922
 WHERE id = '64f88c44-fa8e-44aa-9d2f-e40b5a0f1898';   -- 23차 이자

UPDATE closing_settlements SET confirmed_amount_krw = -1536433, updated_at = now()
 WHERE id = 'fce21fc1-26c3-4902-92ad-9d549fabf5ef';
```

통관환율은 DB의 **1467.1이 맞다.** `최종정산.csv`의 「입고시 신고환율 1,467.00」은 반올림 표기이고, `중간정산.csv`의 총 수입비용 123,038,342원은 1467.1로만 나온다.

### 6·7차 중간정산은 종결됐다 (2026-09-01 처리 완료)

세 가지가 겹쳐 있었고 전부 규명·적용했다. **지금은 재계산 차이 0이다.**

| 차수 | 원인 | 조치 | 현재 확정금액 |
|---|---|---|---|
| 6차 | 통관 항목(통관보수료 173,800 + 검역 110,000)이 통째로 미적재 | 원본 문서 값으로 적재 + 확정금액 갱신 | 113,128,700 |
| 7차 | 위 + **`transactions.import_amount_usd`에 8차 값 75,829.5가 복사**돼 있었다 | 79,200으로 정정(금액 변동 없음) + 통관 항목 적재 | 115,504,800 |

7차 재계산값은 원본 문서 115,504,856의 절사값과 정확히 일치한다 — USD 오입력이 맞았다는 증거다.
되돌릴 값은 6차 112,844,900 / 7차 115,217,700.

⚠️ **6·7차 수입부가세(6차 10,530,210 / 7차 10,751,980)는 일부러 넣지 않았다.**
`inclusive` 모드는 비용을 빼지 않고 전부 더하므로 지금 넣으면 그대로 공급가에 얹힌다.
**`exclusive` 전환(7번 3항)과 반드시 같이 넣을 것.**

### `inclusive` 잔존 3건 — 2·6·7차뿐이다

라이브 DB 전체에서 `inclusive`인 중간정산은 이 셋뿐(나머지 38건은 `exclusive`).
원본 CSV 마지막 열이 「+ 부가세 제외(7%)」= 공급가인데, 그 값이 그대로 확정금액이 되어 **매출부가세 10%가 빠졌다.**

| 차수 | 과세 항목 | `inclusive`가 만든 증상 |
|---|---|---|
| 2차 | 4건 · 부가세 58,446 | `itemVat`이 버려져 +58,400 |
| 6·7차 | 없음 (오션마스터 인보이스 VAT 열 전부 0) | 항목 부가세 영향 없음 — 통관 항목 미적재가 별개 원인이었다 |

`exclusive` 전환만 했을 때 2차는 공급가 119,244,200 / 부가세 11,924,420 / 합계 131,168,620 이 되고,
**공급가가 현재 확정금액과 원 단위까지 일치**한다 — 저장값은 이미 항목 부가세까지 반영된 공급가이고 매출부가세만 빠져 있었다는 뜻이다.

### 5차 — 트럭비 하나뿐 (서류 대기)

`116,586,250 = 105,987,500(공급가) + 10,598,750(매출부가세)`. 담당자 수기값 106,306,517과의 차이는 **트럭비 319,000과 절사 17원뿐**이다.
DB 5차는 원본 CSV와 완전 일치하고 **트럭비는 원본 문서에도 없다** — 서류 누락이다.
5·6·7차 트럭비를 반영한다면 **부가세 제외 금액**으로 넣어야 한다(5차 290,000 / 6차 810,000 / 7차 308,000).

### 37차 — 실질 쟁점은 수입금액 기준 하나

시스템 211,988,700과 담당자 수기 174,903,180의 차이는 셋으로 전부 설명된다:

```
174,903,180 + 17,813,850(수입금액 11,280$ 차이) + 19,271,700(매출부가세) − 30 = 211,988,700
```

DB는 119,412$(수기금액)를 쓰고 담당자 확인 요청분은 통관금액 119,452$로 **40$ 차이**(× 1.05 × 1504.04 ≈ 63,170원)뿐이다.
DB 수입부가세 역산 과세가격(180,404,200원 ÷ 1504.04 = CIF 119,946$)에서 운임·보험 약 1,116$를 빼면 118,831$로 **어느 쪽과도 정확히 맞지 않는다.**
신고필증 2건(428·429)을 받아 과세가격·환율을 직접 봐야 한다.
위 표의 −44,880(통관보수료·검역수수료 부가세 40,810 미반영)은 **이 건과 무관한 별건**이다.
(40,800 × 1.10 = 44,880 으로 산수가 정확히 떨어진다. 환율과도 무관하다.)

**신고가 2건이고 과세환율이 서로 다르다 (2026-09-02, 유니패스 오픈API로 확인).**

| B/L | 컨테이너 | 카톤 | 수입신고번호 | 수입신고일 | 관세청 고시환율 |
|---|---|---|---|---|---|
| KULFE2600**429** | NSSU7161942 | 3,760 | 02012014260180893A | 2026-05-29 | **1504.04** (05-24 적용개시) |
| KULFE2600**428** | NSSU0215292 | 1,380 | 02012014260180889A | 2026-06-01 | **1507.15** (05-31 적용개시) |

DB `customs_exchange_rate` 는 1504.04 하나뿐이다 — **429 기준**이고 428분에는 1507.15가 적용됐어야 한다.
카톤 비율(1,380/5,140 = 26.85%)로 추정한 영향은 **약 11.5만원**
(32,057$ × 3.11 × 1.05 × 1.10). 카톤 수와 금액 비율이 같다는 보장이 없으므로 **추정치**이고,
정확한 값은 신고필증 428·429의 과세가격을 봐야 나온다 (아래 「서류 대기」 8번과 같은 건).

구조적으로 `transactions.customs_exchange_rate` 가 차수당 1개라 **한 차수에 신고가 2건이고
주가 갈리면 표현할 방법이 없다.** 37차가 그 케이스다. 서류 확인 후 스키마를 손댈지 판단한다.

### 1·2차 재입력 시 값

부담비율 50% 적용: 1차 기타 미정산 44,000 → 22,000 / 2차 333,400 → 166,700. 규약대로의 값이므로 시스템 재계산값으로 맞추면 된다.

**단, 이 항목들을 클로징에 두는 것 자체가 재검토 대상이다 (담당자 컨펌 대기).** 담당자는 중간정산으로 옮기자고 제안했다 — 1차 운송추가운임 44,000(디머리지·대기료), 2차 통관보수료 182,600 + 검역수수료 110,000 + 운송추가운임 40,800 = 333,400. 성격상 통관·운송 실비가 맞다.

**옮기면 에이원 부담이 50% → 100%가 된다.** 클로징은 부담비율 50%가 걸리지만 중간정산 통관비는 전액 반영이기 때문이다. 실질 부담 증가분 **1차 +22,000 / 2차 +166,700** (부가세 별도). 담당자 보고에는 이 부담 증가가 언급돼 있지 않으므로 명시해서 확인받을 것. 컨펌 결과에 따라 재입력 방식이 갈린다.

A+B+C ≠ 0 인 차수는 1·2차뿐이므로 (라이브 DB 전수 확인) 영향 범위도 이 둘로 끝난다.

1차는 **선지급금 백필**도 함께 넣는다. 저장된 LC결제비용 72,782,080원은 클로징환율 1202.4로는 나오지 않고, 선지급금 7,040$에 1156.4를 적용하면 원 단위까지 맞는다(`7,040 × 1156.4 + 53,760 × 1202.4`). 금액 변동 없음.

```sql
UPDATE closing_settlements
   SET lc_payment_total_usd = 60800, advance_payment_usd = 7040, advance_exchange_rate = 1156.4
 WHERE id = '0afc9658-046c-413b-b66e-1d195af87550';
```

---

## 3. 원본 문서의 좌측 「LC 이자비용」 열 — 은행서류 차수 순으로 적혀 있다

**원본을 복사하다 밀린 것이 아니다.** `_source_docs/최종정산.csv`는 차수 라벨에 은행서류 차수를 이미 병기해 두고 있다.

```
23 차(은행서류 24차)    24 차(은행서류 23차)
25 차(은행서류 27차)    27 차(은행서류 25차)
```

좌측 「LC 이자비용」 열만 **은행서류 차수 순**으로 적혀 우리 차수와 어긋난 것이고, 우측 「LC 정산 내역」의 **「수수료」가 정본**이다. 수수료 = 인수 + 이자, 여기에 우측 부대비용(개설·조건변경·기타)을 더하고 환급을 빼면 DB 부대비용 합계가 된다.

**DB는 이미 우측 기준으로 정확히 들어가 있다.**

| 차수 | 서류 우측 (수수료 + 부대비용 − 환급) | DB 부대비용 합계 | 판정 |
|---|---|---|---|
| 21차 | 1,716,258 | 1,716,258 | 일치 |
| 23차 | 3,471,200 | 3,467,397 | **−3,803** (2번 (나) 참조) |
| 24차 | 3,278,174 | 3,278,174 | 일치 |
| 27차 | 1,520,140 | 1,520,140 | 일치 |
| 28차 | 3,325,747 | 3,325,747 | 일치 |

**21·24·25·27·28차는 재입력 대상이 아니다.** 고칠 것은 23차 이자 한 줄뿐이다. 좌측 열은 읽지 말 것 — 24차 좌측 「LC 제비용 총합」 3,498,596은 좌우 어느 쪽과도 맞지 않는다.

---

## 4. 계산 코드 정합 — 2026-09-01 전수 점검

**규약의 원본은 `lib/calculations/` 다.** 엑셀·원본 문서가 아니라 이 코드가 기준이고,
화면·PDF·대시보드는 여기를 **호출만** 해야 한다. 공식을 다시 쓴 곳에서 실제로 차이가 나 있었다.

| 위치 | 무엇이 틀렸나 | 조치 |
|---|---|---|
| `lib/data/dashboard.ts` 검증 이슈 차액 | `(수입금액 + 비용합계) × 1.10` 으로 재계산 — **항목 부가세·수입부가세 공제·절사 정책·`vat_mode` 를 전부 무시**했다. 규약대로 저장된 차수도 차액이 있는 것처럼 떴다 | `calculateInterim` 호출로 교체 (`lib/calculations/settlementDiff.ts` + 회귀 테스트 6건) |
| `lib/pdf/ClosingPdfTemplate.tsx` 환율 민감도 표 | LC 결제액 대신 **수입금액USD** 로 환산했고, `round(부담분 × 1.1) + 기타비용` 이라는 **구방식 공식이 박혀** 있었다. 절사·`vat_mode` 도 무시 | `calculateClosing` 호출로 교체 (`lib/pdf/closingSensitivity.ts` + 회귀 테스트 8건) |
| `components/report/ReportInterimSection.tsx` | 공급가 대체 계산식에서 **항목 부가세가 빠져** 있었고 화면의 계산식 문자열에도 표시되지 않았다 | `costItemVat`·`computeVat` 사용으로 교체, 계산식에 「+ 항목 부가세」 표기 추가 |
| `lib/pdf/InterimPdfTemplate.tsx` 원가 계산표 | 나열한 항목을 다 더해도 아래 「공급가」가 안 나왔다 (항목 부가세 줄 없음) | 「항목 부가세 가산」 행 추가 (`lib/pdf/interimRows.ts` + 회귀 테스트 6건) |
| `components/report/*` 부가세 | `Math.round(x * 0.1)` 이 두 곳에 복제 | `computeVat` 로 통일 |
| `app/(app)/verification/page.tsx` | 대상 차수가 **`rn <= 33` 으로 하드코딩** — 34차 이후가 조용히 빠졌다 | `source_documents` 에 적재된 차수 전부로 변경 |

### 계산에 쓰이지 않던 입력값

`closing_cost_items.includes_vat`(화면의 「부가세 포함」 체크)는 **`calculateClosing` 이 읽지 않는다.**
규약(`기타 미정산 비용 × 부담비율`)에 그 구분이 없어서 테스트도 전액 포함을 기대하고 있다.
계산 시그니처에서는 뺐고 DB 컬럼과 체크박스는 기록용으로 남겼다.
**체크가 금액을 바꾸지 않는다는 점을 담당자에게 알릴 것** — 바꿔야 한다면 규약부터 정해야 한다.

---

## 5. 알려진 시스템 상태

### 감사 로그

- 트리거 **7개** 동작 중 — `transactions` / `interim_settlements` / `closing_settlements` / `lc_fee_items` / `closing_cost_items` / `interim_cost_items` / `containers` (마이그레이션 025 + 030)
  - 부모 3개만으로는 **LC수수료(`lc_fee_items`) 편집을 놓친다.** 13·17차 차이의 정체가 이것으로 추정된다
- `audit_logs` 는 앱에서 읽기 전용 — anon/authenticated의 INSERT/UPDATE/DELETE 회수됨. 트리거 함수는 SECURITY DEFINER라 기록은 계속된다
- **`AUTH_DISABLED` 때문에 `changed_by`는 항상 NULL.** 「무엇이 바뀌었나」만 남고 「누가」는 안 남는다
- ⚠️ **anon PostgREST로 `audit_logs`를 조회하지 말 것.** 항상 `200 + []`(0건)이 나온다 — 권한 거부가 아니라 RLS가 걸러낸 결과다
  - `audit_logs`는 **RLS가 켜진 유일한 테이블**이고(`relrowsecurity = true`), SELECT 정책이 `a1_admin can select audit_logs`(`authenticated` + `auth_role() = 'a1_admin'`) 하나뿐이다
  - 같은 이유로 앱의 감사 로그 화면(`app/(app)/audit-logs`)은 anon 클라이언트를 쓰는 한 **영원히 빈 화면**이다. 중복이던 `settings/audit` 화면과 쓰이지 않던 `app/api/audit-logs` 는 삭제했다 (2026-09-01)
  - **근본 해결은 로그인 복구(AUTH_RESTORE).** anon SELECT 정책을 여는 것은 공개 키를 가진 누구에게나 감사 이력을 여는 것이라 권하지 않는다
  - 건수 확인은 **SQL 에디터로만** 가능

### 그 외

- ✅ **항목 부가세를 공급가에 포함하는 수정(`f2e2be3`)은 `origin/main`에 배포돼 있다.** `feat/interim-supply-basis-and-override-guard` 브랜치는 이미 머지됐고 오히려 main보다 뒤처져 있다
  - 다만 37차 중간정산은 여전히 **−44,880** 차이가 남아 있고 원인이 다르다. 저장된 공급가 192,717,000은 트럭운송비 부가세 115,370만 반영하고 **통관보수료·검역수수료 부가세 40,810이 빠져 있다.** 규약대로면 항목 부가세 156,180 전액이 들어가 공급가 192,757,800 / 합계 212,033,580이 맞다
  - 35~43차 중간정산 재계산 결과 **37차를 뺀 전부가 차이 0**
- **정산 방식(`vat_mode`) 전환 UI 없음** — 라이브 DB의 클로징 33건이 전부 `inclusive`다. 29~34차를 신방식으로 소급하려면 DB 직접 수정 또는 화면 추가 필요
- ⚠️ **정산 잠금(`is_locked`)은 화면에서만 걸린다.** 018 마이그레이션의 잠금 정책은 RLS 정책인데 `interim_cost_items` 는 RLS가 꺼져 있어 무력하다 — 확정된 정산도 PostgREST로는 항목이 그대로 수정된다
- **차수 라벨 연도 오류 다수** — 1~10·16·17·18·21·23·25·27·34차. 라벨만의 문제이고 금액과는 무관하다
  - 34차 통관일 역전은 **해결됨** (2026-09-02, 유니패스 대조). 25차는 남아 있다

---

## 6. 데이터 미비 차수

| 차수 | 상태 |
|---|---|
| 20차 | 클로징 미확정 (서류 요청 중) |
| 25차 | 중간·클로징 모두 없음 (서류 요청 중) |
| 42차 | 중간정산 없음, 통관일 미입력 (서류 요청 중) |
| 44차 | 데이터 전무 |
| 35~41·43차 | 중간정산만 있고 클로징 없음 (정상) |

---

## 7. 남은 작업

### 유니패스 오픈API 대조 (2026-09-02 반영 완료)

관세청 유니패스 오픈API로 43건을 전수 대조했다. 29건 조회 성공, 14건은 보관주기 3년 초과로 조회 불가.

- **통관환율 42건 중 41건이 관세청 고시환율과 소수점까지 일치**했다. 담당자 입력은 정확하다.
- 정정한 것 (`supabase/manual/manual_unipass_reconcile_20260902.sql`, 실행 완료):
  - 34차 통관일 `2025-09-26` → `2026-03-06` (32차 값이 복사돼 있었다. 환율 1439.26 은 맞아 **정산금액 영향 없음**)
  - 42차 통관일 `NULL` → `2026-08-25`, 통관환율 `NULL` → `1405.43` (2026-08-23 적용개시 주)
  - 36차 컨테이너 `KTMU9446283` → `KMTU9446286`. **ISO 6346 체크디짓만으로는 못 잡는다** — 잘못 옮긴 번호도 우연히 유효했다
  - Master B/L 29건 채움 (전부 NULL 이었다). 이제 선사 사이트 직접 조회가 가능하다
- 반영하지 않은 것: ETA 12건. DB는 예정일, 유니패스는 실제 입항일이라 성격이 다르다 — 덮어쓰면 지연 여부를 잃는다
- **API 한계**: 출항일(ETD)은 제공하지 않는다(입항일까지). 보관주기 3년. `수입 제세 납부여부`(API) 는 어떤 신고번호를 넣어도 서버 오류라 사용을 접었다

### 담당자 컨펌 대기

1. **23차 정정** — 이자 2,642,119 → 2,645,922 + 확정금액 −59,350,700 → −1,536,433 (위 2번 SQL 2문)
2. **1·2차 기타 미정산 비용을 중간정산으로 옮길지** — 옮기면 에이원 부담 50% → 100%(1차 +22,000 / 2차 +166,700). 답이 나와야 1·2차 재입력 방식이 정해진다
3. **2·6·7차 매출부가세 부과 여부** — `inclusive` 잔존 3건. 약 +3,490만(2차 11,924,420 / 6차 11,401,970 / 7차 11,584,360). **금액이 가장 큰 미해결 건**이고 이미 발행된 세금계산서와의 정합 확인이 먼저다. 전환 시 6·7차 수입부가세도 같이 적재할 것 (2번 참조). 세 건이 같은 원인이므로 한 번에 처리한다

### 컨펌 불필요 — 사용자 승인만 받으면 실행

4. **6차 확정금액** −100, **37차 확정금액** −44,880 — 둘 다 시스템이 맞다. 확정금액만 맞추면 끝
5. **1차 선지급금 백필** — 금액 변동 없음 (위 2번 SQL)
6. **37차 과세환율 2건 분리** — 신고가 두 주에 걸쳐 1504.04/1507.15 로 갈린다. 영향 추정 약 11만원.
   신고필증 428·429의 과세가격을 받아야 확정된다 (아래 「서류 대기」와 같은 건)

### 서류 대기

6. **7차 클로징 원인 규명** — 원본 서류 수령 후. 중간정산 쪽은 USD 오입력 정정으로 종결(2번 참조)
7. **5·6·7차 트럭비** — 원본 문서에 없다. 서류 확보 + 담당자 컨펌 필요 (2번 참조)
8. **37차 통관 신고필증(428·429)** — 수입금액 기준 확정용 (2번 참조)
9. **20·25·42차 서류** — 25차는 22차 자료가 잘못 와 있어 재요청 중. 44차는 9월 말 입항 이후

### 선택

10. **로그인 복구(AUTH_RESTORE)** — 감사 로그 화면을 살리려면 필요. RLS를 켜면 앱이 즉시 죽으므로 반드시 같이 해야 한다

---

## 8. 조사 방법 (재현용)

라이브 DB 대사는 아래 순서로 한다. **anon PostgREST로 조회 가능한 테이블**은 `transactions` / `interim_settlements` / `closing_settlements` / `lc_fee_items` / `closing_cost_items` 등 대부분이고, `audit_logs`와 `pg_catalog`만 SQL 에디터가 필요하다.

1. `.env.local`의 `NEXT_PUBLIC_SUPABASE_ANON_KEY`로 PostgREST 조회 (조인은 `transactions?select=...,closing_settlements(...)`)
2. `node ./scratch.ts` 로 `lib/calculations/closing.ts`의 `calculateClosing`을 **실제 코드 그대로** 돌려 재계산 — 파이썬으로 공식을 다시 구현하지 말 것
3. DB 확정금액 − 재계산값으로 차이를 낸다

통관 사실관계(입항일·수입신고일·선사·컨테이너번호·Master B/L)는 **관세청 유니패스 오픈API**로 확인한다.
`lib/tracking/unipass.ts`(화물통관진행정보) / `lib/tracking/customsRate.ts`(관세환율)를 그대로 쓰면 되고,
인증키는 `.env.local`의 `UNIPASS_API_KEY_CARGO` / `UNIPASS_API_KEY_FXRATE` 다.
**출항일(ETD)은 제공하지 않고, 보관주기 3년이 지난 건은 조회되지 않는다.**

DDL·`audit_logs`·카탈로그 조회는 SQL 에디터에서 실행해야 한다. **Supabase SQL 에디터는 여러 문장을 실행하면 마지막 문장의 결과만 보여준다** — 여러 조회를 한 번에 받으려면 `UNION ALL` 로 단일 SELECT를 만들 것.

---

## 9. 유의

- 담당자가 라이브 DB를 실시간으로 편집 중이다. 감사 결과는 스냅샷이므로 수치가 어긋나면 재조회한다 (예: 37차 항목 부가세가 152,470 → 156,180으로 변경된 사례)
- 확정금액을 바꾸는 SQL은 **실행 전 사용자에게 보여주고 승인받는다.** 금액이 큰 건(23차 5,780만원 등)은 담당자 컨펌까지 받는다
- 「확정금액 미갱신」 = 저장 후 LC수수료·결제비용·환율을 고쳤는데 확정금액이 재계산되지 않아 옛 값이 굳은 경우. 화면에서는 `OverrideMismatchNotice`가 경고와 되돌리기를 제공한다

---
